---
"yunxing": minor
---

Add OpenCode plugin support so the repo installs via `opencode plugin --global "yunxing@git+https://github.com/raptoravis/yunxing.git"`, replacing the `skills.paths` link. Register promoted skills as custom commands so `/cap`, `/ask-matt`, and the other skill commands appear in the OpenCode 1.x slash catalog.
